{
  "name": "ai-chat-markdown-exporter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for exporting ChatGPT, Claude, and Gemini conversations as Markdown.",
  "scripts": {
    "icons": "python3 scripts/generate-icons.py",
    "build": "node scripts/build.mjs",
    "check": "node --check src/markdown-utils.js && node --check src/content-script.js && node --check src/popup.js && node --check scripts/build.mjs && node --check tests/extractor.test.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [
    "chrome-extension",
    "markdown",
    "chatgpt",
    "claude",
    "gemini"
  ],
  "license": "MIT",
  "devDependencies": {
    "linkedom": "^0.18.12"
  }
}
